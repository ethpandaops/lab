export interface NavBarProps {
  /**
   * Whether to show breadcrumbs on the left
   * @default true
   */
  showBreadcrumbs?: boolean;
  /**
   * Whether to show navigation links on the right
   * @default true
   */
  showNavLinks?: boolean;
}
