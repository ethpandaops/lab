export interface NetworkIconProps {
  /**
   * Network name (e.g., "mainnet", "holesky", "sepolia")
   */
  networkName: string;

  /**
   * Optional size class for the icon (default: "size-5")
   */
  className?: string;
}
