export interface NetworkSelectorProps {
  /**
   * Whether to show the label above the selector
   * @default true
   */
  showLabel?: boolean;

  /**
   * Custom label text
   * @default "Network"
   */
  label?: string;
}
