// Export all types from the google.protobuf namespace
import * as proto from '../../proto';

export const {
  Timestamp,
} = proto.google.protobuf;

// Export interfaces
export type ITimestamp = proto.google.protobuf.ITimestamp;
