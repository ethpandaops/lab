version: v2
plugins:
  # TypeScript message type generation for all protos
  - remote: buf.build/bufbuild/es:v1.10.0 # v2.3.0 does not work with connect-web
    out: frontend/src/api/gen
    opt: target=ts